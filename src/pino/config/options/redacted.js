const redacted = [
  user.firstname, 
  user.lastname,
  user.age, 
  user.password, 
  user.email,
  admin, 
  admin.firstname,
  admin.lastname,
  admin.age,
  admin.password,
  admin.email,
  censor, 
  censored,
  secret, 
  secret.message,
];

export {redacted};